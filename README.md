# TS3-to-TS6-server-groups-transfer
TeamSpeak 6 has changed the hashing method for UIDs, so a direct conversion from the TeamSpeak 3 db is not possible. This tool can automate the migration process to TS6. The user logs into both TS3 and TS6 simultaneously, and the app transfers their ranks based on IP address.
